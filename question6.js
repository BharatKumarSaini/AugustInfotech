// Complete this statement to keep track of a "count" variable using the useState
// Hook. [1 mark]
import { useState } from "react";

function KeepCount() {
  const [ count , setCount ] = useState(0);
}